# Status Update — 2026-03-15 — Batch 01

## What shipped

- Request backlog scope alignment completed:
  - #9 marked deferred (hardcoded installer retained intentionally)
  - #23 deferred from core scope (plugin-only concept)
- Macro syntax-coloring fix delivered:
  - `.FCMacro` now maps to Python highlighting
  - markdown false-positive sniffing reduced for macro comment headers
- Undefined identifier visual differentiation delivered:
  - PY301/PY302 diagnostics now tint undefined identifiers in-editor
- Settings Syntax Colors table readability improvements delivered:
  - token label column width rebalanced
  - full-token tooltips added

## What changed

- Tree-sitter language detection now prefers Python signal before markdown in ambiguous extensionless cases.
- Editor language sniffing uses a larger content sample window.
- Diagnostics span extraction now uses identifier-length parsing for pyflakes undefined-name diagnostics.
- Settings dialog syntax table now uses fixed utility columns with a protected token column.

## What’s next

- Final docs/process pass:
  - complete macro workflow guidance wording updates
  - mark remaining TODO statuses based on shipped behavior
- Run focused cross-slice test suite and sync changelog/TODO evidence.

## Known risks/blockers

- #9 remains deferred pending a proven relocation-safe installer approach in ChoreBoy production constraints.
- #23 remains out of core scope until/unless plugin-specific requirements are defined.

## Evidence links

- Tests:
  - `python3 run_tests.py -v --import-mode=importlib tests/unit/editors/test_syntax_registry.py tests/unit/intelligence/test_semantic_tokens.py`
  - `python3 run_tests.py -v --import-mode=importlib tests/unit/intelligence/test_diagnostics_service.py tests/unit/editors/test_code_editor_widget_highlighting.py`
  - `python3 run_tests.py -v --import-mode=importlib tests/unit/shell/test_settings_dialog.py`
- UI evidence:
  - `/tmp/computer-use/581f5.webp` (dark mode syntax colors readability)
  - `/tmp/computer-use/eafea.webp` (dark mode applied across shell)
- Backlog/docs:
  - `docs/USER_REQUESTS_TODO.md`
  - `docs/STATUS_UPDATES.md`
