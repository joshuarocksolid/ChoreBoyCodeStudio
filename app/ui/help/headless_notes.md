# FreeCAD Headless Notes

- Some GUI-dependent FreeCAD operations do not work in console/headless mode.
- If you hit `Cannot load Gui module in console application`, switch to a headless-safe API path.
- Keep your run logs for diagnostics and support bundle generation.
